# Saída de build: lib/ é gerado pelo UglifyJS e dist/ pelo tsc.
lib
dist

# Certificados (gitignored, mas podem existir localmente).
certs

# Gerado pelo npm; reformatar brigaria com o próprio npm a cada install.
package-lock.json
