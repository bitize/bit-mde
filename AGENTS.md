# AGENTS.md

As regras deste repositório vivem em [CLAUDE.md](CLAUDE.md), e valem para qualquer agente — não só para o Claude Code. Lá estão o que é o projeto, os comandos, o pré-requisito de `certs/` para os testes, a CI, a arquitetura em camadas, as convenções que atravessam o código e o processo de release.

Este arquivo é só um ponteiro, de propósito: duplicar o conteúdo aqui garante que as duas cópias divirjam.
